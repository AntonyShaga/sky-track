import FlightInfo from "./FlightInfo.tsx";

const FlightInfoPanel = () => {
  return (
    <div className={"bg-neutral-900 p-5 rounded-lg h-fit"}>
      <FlightInfo />
    </div>
  );
};

export default FlightInfoPanel;
